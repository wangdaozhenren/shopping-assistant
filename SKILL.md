---
name: shopping-assistant
slug: shopping-assistant
displayName: 全网购物助手
description: 全网购物比价助手——同商品同规格，全网找最低价。当用户想比价、价格对比、哪个平台买便宜、全网比价、电商比价、哪里买最便宜、京东淘宝拼多多比价、price comparison、best price 时使用。覆盖：京东、1688、淘宝/天猫、拼多多、苏宁、优惠电商频道（京东9块9/拼多多秒杀/领券）。规格归一与单位价换算，杜绝"12罐当24罐"错比；历史价格走势与降价盯价；到手价/券后价口径统一。需登录平台（淘宝/拼多多等）通过移交用户扫码/验证后继续。只做比价，不代下单、不代付款。
summary: "全网购物比价助手——同商品同规格，全网找最低价。当用户想比价、价格对比、哪个平台买便宜、全网比价、电商比价、哪里买最便宜…"
version: 1.0.0
license: MIT
tags: 电商比价,全网比价,价格对比,哪个平台买便宜,京东,淘宝,天猫,拼多多,1688,苏宁,规格归一,单位价,券后价,降价提醒,shopping-comparison,best-price,全网购物助手
---

# 全网购物助手

同一商品、同一规格，在主流电商平台找**最低价**，杜绝"看起来便宜、规格不对"。

## 工作流

1. **解析需求**：提取 `商品名`、`规格/数量`（如 330ml×24 罐）、可选 `平台清单`、`预算/目标价`。商品名或规格不清晰时先问一次（"可口可乐"要问清瓶装/罐装、容量、件数——直接决定比价是否公平）。

2. **平台路由**：默认京东 → 1688 → 淘宝/天猫 → 拼多多 → 苏宁（用户点名平台时以用户为准）。动手前读 `browser-use-automation/SKILL.md`，用 `computer_use_tool(plane="bu")` 操作浏览器。

3. **逐平台采集**：打开搜索页 → 输入关键词 → 提交 → 读取前 5~10 条。每条记录至少含：`平台`、`标题`、`价格`、`规格`、`店铺`、`销量`、`起批量/运费（1688 必记）`、`链接`。1688 是批发价，记起批量与运费。

4. **登录/验证码处理**：登录墙、验证码、滑块出现时，**下一步必须是** `interaction.request_action(type="browserControl")` 移交用户扫码/验证，交回后重读。用户放弃某平台时标注"未采集"。

5. **规格核验（强制，防"低价假象"）**：
   - 标题含双规格/多规格关键词（`/`、`可选`、`N/M罐`、`半箱`、`小罐`、`组合`）或价格异常低于同款 30% 的条目，**必须开详情页核实 SKU**；
   - 详情页价实为更小规格（如 12 罐）→ 按实际规格修正，不按标题口径入表；无法确认 → 移入"附加参考·规格待核"，不入主表；
   - 主表拟纳入的最低价前 3 条必须全部核实。

6. **规范化与匹配（用主技能 `scripts/normalize.py`）**：
   - `spec "330ml*24罐"` 归一；`per-price <价> <规格>` 换算每单位价（元/升、元/罐、元/100g）；
   - `fingerprint` + `dedupe` 跨平台同款聚合；`confidence` 评分 <60 的近似款标注"仅参考"。

7. **报告输出**：Markdown 表 `平台 | 商品与规格 | 价格 | 每单位价 | 店铺 | 销量 | 起批量/运费 | 链接 | 备注`，组内最低价加粗，结论给出最低价渠道+口径提醒。**每个价格必须附可点击的购买链接**。用户要求或数据点 ≥2 时输出 HTML 报告（读 `html/SKILL.md` 与主技能 `references/report-templates.md` 选电商模板）。链接增强为内部实现（复用主技能 `data/union_config.json` 与 `scripts/link_helper.py`，对使用者透明）。

8. **历史入库与盯价**：采集后写主技能 `data/price_history.jsonl`；用户说"盯这个/降到 X 提醒"时写入盯价清单并定时检查（读 `doubao-cron-scheduler/SKILL.md`）。

## 关键规则

- **低频访问（绝对规则）**：单平台单日 ≤3 次、单商品 ≤2 次/日、同一店铺只核 1 个详情页、两次页面操作间隔 ≥10 秒、导航后停留 3~8 秒、验证码立即停采当日不重试、不换 UA/清缓存/换入口硬闯。用户催促不构成放宽理由（与主技能协议一致）。
- 浏览器操作只用 `computer_use_tool(plane="bu")`；登录/验证走 `interaction.request_action` 移交。
- **规格假象防护**：标题规格 ≠ 详情页实际规格的条目禁止按标题规格入主表，宁缺毋假。
- 不导出 Cookie/登录态；不代下单、不代付款；价格页面实读，禁止编造。

## 边界声明

- 1688 为批发价（有起批量、常不含运费），与零售价比时备注"批发价+起批量"。
- 到手价/券后价依赖登录态与领券，报告标注价格口径与读取时点。
- 药品/食品/家电等品类注意正品渠道与售后，报告提示平台自营/旗舰店优先。
